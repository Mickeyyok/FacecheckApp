<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ สิทธิ์ที่จำเป็น -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- 📷 กล้อง -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <!-- 📍 สิทธิ์ตำแหน่ง (จำเป็นสำหรับเช็คระยะ + Google Maps) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_utcc_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_utcc_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacecheckApp">

        <!-- ⭐⭐⭐ ใส่ Google Maps API KEY ที่นี่ ⭐⭐⭐ -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDUWll66qkfmJPblkl9Sjx9bMWoLpsbPPU"/>
        <!-- ⭐⭐⭐ END ⭐⭐⭐ -->

        <activity android:name=".PersonalActivity" android:exported="false" />
        <activity android:name=".SettingActivity" android:exported="false" />

        <!-- หน้าเริ่มต้น -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ConsentActivity" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".RegScanActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AddSubjectActivity" />
        <activity android:name=".TeacherHomeActivity" />
        <activity android:name=".CreateClassActivity" />
        <activity android:name=".CreatetimeActivity" />
        <activity android:name=".ClassDetailActivity" />
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".StudentListActivity" />
        <activity android:name=".EditClassActivity" />
        <activity android:name=".EditTimeActivity" />
        <activity android:name=".SubjectListActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".FaceScanActivity" />
        <activity android:name=".LocationCheckActivity" />
        <activity android:name=".CheckinSuccessActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".RealTimeActivity" />
        <activity android:name=".TermActivity" />

    </application>

</manifest>
