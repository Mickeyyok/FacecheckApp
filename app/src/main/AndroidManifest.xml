<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ สิทธิ์ที่จำเป็น -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <!-- ✅ ระบุว่าอุปกรณ์นี้ต้องมีกล้อง -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacecheckApp">

        <!-- ✅ หน้าเริ่มต้นของแอป -->
        <activity
            android:name=".HistoryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ หน้าล็อกอิน -->
        <activity android:name=".LoginActivity" />

        <!-- ✅ หน้าลงทะเบียนใบหน้า (ถ่ายรูป/บันทึกข้อมูลใบหน้า) -->
        <activity android:name=".RegisterActivity" />

        <!-- ✅ หน้ายินยอมการใช้งาน / นโยบาย -->
        <activity android:name=".ConsentActivity" />

        <activity android:name=".SuccessActivity" />

        <activity android:name=".RegScanActivity" />

        <activity android:name=".HomepageActivity" />

        <activity android:name=".AddSubjectActivity" />

        <activity android:name=".TeacherHomeActivity" />

        <activity android:name=".CreateClassActivity" />

        <activity android:name=".CreaetimeActivity" />

        <activity android:name=".ClassDetailActivity" />

        <activity android:name=".AddStudentActivity" />

        <activity android:name=".StudentListActivity" />






    </application>

</manifest>
