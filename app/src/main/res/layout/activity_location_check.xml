<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- 🔵 Header Gradient -->
    <RelativeLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="85dp"
        android:background="@drawable/bg_gradient">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_marginStart="20dp"
            android:layout_marginTop="38dp"
            android:src="@drawable/ic_arrow_back_white"
            android:tint="@android:color/white" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="35dp"
            android:text="ตำแหน่ง"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:textStyle="bold" />
    </RelativeLayout>


    <!-- 🗺 Google Map -->
    <FrameLayout
        android:id="@+id/mapContainer"
        android:layout_below="@id/header"
        android:layout_width="match_parent"
        android:layout_height="330dp">

        <fragment
            android:id="@+id/mapFragment"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>


    <!-- 📘 การ์ดวิชา -->
    <LinearLayout
        android:id="@+id/cardSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/mapContainer"
        android:layout_margin="20dp"
        android:orientation="vertical"
        android:padding="18dp"
        android:background="@drawable/bg_card">

        <!-- ✨ รายละเอียดวิชาที่ Kotlin ใช้ -->
        <TextView
            android:id="@+id/tvSubjectDetail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="รหัสวิชา:  ชื่อวิชา: "
            android:textSize="18sp"
            android:textColor="@android:color/black"
            android:textStyle="bold" />
    </LinearLayout>



    <!-- ✨ ข้อความแจ้งเตือน -->
    <TextView
        android:id="@+id/tvNotice"
        android:layout_below="@id/cardSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="**เพื่อป้องกันการทุจริตระบบจำเป็นต้องตรวจสอบว่าคุณอยู่ในพื้นที่มหาวิทยาลัยก่อนเริ่มสแกนใบหน้า**"
        android:textAlignment="center"
        android:textSize="13sp"
        android:paddingHorizontal="30dp"
        android:layout_marginTop="-5dp"
        android:layout_marginBottom="10dp" />


    <!-- 🔵 ปุ่มเริ่มสแกน -->
    <Button
        android:id="@+id/btnStartScan"
        android:layout_width="260dp"
        android:layout_height="55dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tvNotice"
        android:backgroundTint="#1976FF"
        android:text="เริ่มสแกน"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginTop="5dp" />


    <!-- 🔘 ข้อความเมื่ออยู่นอกระยะ -->
    <TextView
        android:id="@+id/tvOutRange"
        android:layout_width="260dp"
        android:layout_height="55dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnStartScan"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:backgroundTint="#D1E7FF"
        android:text="• คุณอยู่นอกระยะ •"
        android:textColor="#0056B3"
        android:textStyle="bold"
        android:textSize="16sp"
        android:visibility="gone" />
</RelativeLayout>
